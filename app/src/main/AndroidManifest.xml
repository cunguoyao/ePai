<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.cmkj.auction" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".app.BaseApplication">
        <activity android:name=".ui.SplashActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.UserInfoActivity" />
        <activity android:name=".ui.UserAccountSafeActivity" />
        <activity android:name=".ui.UserAddressActivity" />
        <activity android:name=".ui.UserAddressAddActivity" />
        <activity android:name=".ui.UserMessageActivity" />
        <activity android:name=".ui.MyOrderListActivity" />
        <activity android:name=".ui.AboutUsActivity" />
    </application>

</manifest>